# 示例插件 (Example Plugin)

这是一个演示 Claude Code 插件基本结构的示例插件。

## 📁 插件结构说明

```
example-plugin/
├── .claude-plugin/
│   └── plugin.json      # 必需：插件清单文件
├── README.md            # 推荐：插件说明文档
├── commands/            # 可选：斜杠命令目录
│   └── hello.md         # 命令定义文件
├── agents/              # 可选：代理目录
│   └── assistant.md     # 代理定义文件
└── hooks/               # 可选：钩子目录
    └── hooks.json       # 钩子配置文件
```

## 🔧 各组件说明

### 1. plugin.json（必需）

位于 `.claude-plugin/plugin.json`，定义插件的元数据：

```json
{
  "name": "example-plugin",        // 插件标识符（kebab-case）
  "description": "插件描述",        // 简短描述
  "version": "1.0.0",              // 语义化版本号
  "author": { "name": "作者名" },   // 作者信息
  "license": "MIT",                // 许可证
  "keywords": ["关键词1", "关键词2"] // 用于搜索发现
}
```

### 2. 命令 (Commands)

在 `commands/` 目录下创建 `.md` 文件，文件名即为命令名：

- `hello.md` → 使用 `/hello` 调用
- `code-review.md` → 使用 `/code-review` 调用

### 3. 代理 (Agents)

在 `agents/` 目录下创建 `.md` 文件，定义专门的AI助手角色。

### 4. 钩子 (Hooks)

在 `hooks/` 目录下使用 `hooks.json` 定义事件触发的自动化操作。

## 💡 快速开始

1. 复制此插件目录作为模板
2. 修改 `plugin.json` 中的插件信息
3. 在 `commands/` 中添加你的命令
4. 测试：`/plugin install example-plugin@mirror-plugins`

## 📚 更多资源

- [Claude Code 插件文档](https://code.claude.com/docs/plugins)
- [创建插件市场](https://code.claude.com/docs/create-marketplaces)
